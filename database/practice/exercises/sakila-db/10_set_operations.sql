-- SQL Set Operations

-- Basic

-- List all first names from both the `actor` and `customer` tables (remove duplicates).
-- List all first names from both the `actor` and `customer` tables, including duplicates.
-- Find city names that appear in both the `address` and `city` tables.
-- Get a combined list of film titles and customer first names.
-- List distinct store IDs from both `inventory` and `staff`.

-- Intermediate

-- List customer last names who have made payments or rented movies.
-- Find film IDs that are in `inventory` but not in `rental`.
-- Find actor IDs who acted in films and customer IDs who rented films.
-- Find staff IDs that appear in both `payment` and `rental`.
-- List film titles that are not present in the `inventory`.

-- Advanced

-- List language IDs used in the `film` table and those present in the `language` table.
-- Find actors who acted in films but never in any ‘Horror’ films.
-- Get email addresses of customers who either rented or made a payment.
-- List all film IDs that are in either `inventory` or `rental`.
-- Find films that were rented but are no longer in inventory.

-- Applied Scenarios

-- Find customer IDs who rented but never made a payment.
-- Find customer IDs who both rented and made a payment.
-- Get film IDs rented by customers who never paid anything.
-- List film IDs that are in inventory more than once and have been rented.
-- Find customer IDs who paid but have no rentals.

-- Mixed Logic

-- Get a list of first names from both staff and customers.
-- Find store IDs associated with staff but not inventory.
-- List all languages used in films and that exist in the `language` table.
-- List film titles that have never been rented.
-- Find actor IDs who acted only in films that were never rented.